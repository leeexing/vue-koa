/**
 * TODO: 待完善
*/
const vueScroll = {}
vueScroll.install = Vue => {
  Vue.directive('scroll', {
    bind (el, binding) {
      //
    },
    inserted (el, binding) {
      //
    },
    update (el, binding) {
      //
    },
    unbind (el, binding) {
      //
    }
  })
}

export default vueScroll
